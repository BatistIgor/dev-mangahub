/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false, 
    images: {
      domains: ['127.0.0.1'], // Добавьте ваш хост
    },
  };
  
  export default nextConfig;
